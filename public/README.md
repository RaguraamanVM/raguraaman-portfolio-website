Drop these two files directly into this `public/` folder:

- `profile.jpg`   — your headshot (square works best; falls back to "RV" initials if missing)
- `Raguraaman_Resume.pdf` — your résumé, served at /Raguraaman_Resume.pdf

Both are already wired up in `src/data.js` — nothing else to configure.
